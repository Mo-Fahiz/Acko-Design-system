# Badge — React

> **Scope:** Web only. No visual drift from [`../badge-base.md`](../badge-base.md).

## Packages

| Artifact | Location |
|----------|----------|
| Components | `@acko/badge` (`Badge`, `CounterBadge`) |
| Styles | `@acko/css/badge.css` |

```tsx
import { Badge, CounterBadge } from "@acko/badge";
```

## Notes

- `CounterBadge` shares styling context with Badge; see `packages/badge/src` for exports.
- Props match `BadgeProps` / counter types in source — use `packages/badge` as truth.

## References

- [`../badge-base.md`](../badge-base.md)
- `packages/css/src/badge.css`
