# Badge — Flutter

> **Scope:** Flutter mapping only. Match [`../badge-base.md`](../badge-base.md); semantic tokens as `colorPurple` / kebab in docs → lowerCamelCase in Dart per project convention.

## Goals

- Chip-like or `Container`-based badge with pill radius; map `color` enum to theme semantic colors.
- **CounterBadge:** display logic for `max` cap (e.g. `99+`).

## References

- [`../badge-base.md`](../badge-base.md)
- [`../../global.md`](../../global.md)
