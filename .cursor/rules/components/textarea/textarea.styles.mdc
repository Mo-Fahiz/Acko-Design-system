---
description: Textarea visual spec — CSS classes, no-resize behavior, max-height scroll, states, and token usage
globs: "**/textarea/**,**/Textarea/**"
---

# Textarea — Styles

CSS file: `@acko/css/textarea.css`

## CSS Classes

### Structure
- `.acko-textarea` — flex column, gap `var(--scale-8)`
- `.acko-textarea-label` — label-lg typography, `--color-text-default`
- `.acko-textarea-el` — the `<textarea>` element

### Sizing & Resize
- Resize: `none` (hard-coded, no resize handle)
- Min height: `var(--scale-120)` (~120px)
- Max height: `calc(4 * var(--font-body-md-line) + 2 * var(--scale-12))` — approx 4 lines
- `overflow-y: auto` — scrolls when content exceeds max-height
- Padding: `var(--scale-12) var(--scale-16)`
- Font: `--font-body-md-size`, line-height 1.5
- Border radius: `--radius-3xl`

### States (on `.acko-textarea-el`)

| State | Border | Background | Extra |
|-------|--------|-----------|-------|
| Default | `--color-input-border` (`grey-150` light / `grey-600` dark) | `--color-input-bg` (`grey-white` light / `grey-750` dark) | — |
| Hover | `--color-input-hover-border` (`grey-200` light / `grey-550` dark) | `--color-input-bg` | Pointer devices only |
| Focus (`.acko-textarea-focused`) | `--color-input-focus-border` | `--color-input-bg` | `box-shadow: 0 0 0 2px var(--color-input-focus-ring)` |
| Error (`.acko-textarea-error`) | Gradient border `linear-gradient(0deg, red-200, grey-150)` via padding-box/border-box (dark: red-800 → grey-600) | `--color-input-bg` | — |
| Success (`.acko-textarea-success`) | `--color-success` | `--color-input-bg` | — |
| Disabled (`.acko-textarea-disabled`) | `--color-input-disabled-border` | `--color-input-disabled-bg` | `cursor: not-allowed`, `--color-text-disabled` |

### Footer
- `.acko-textarea-footer` — flex justify-between
- `.acko-textarea-helper-text` / `.acko-textarea-error-text` — `--font-caption-size`
- `.acko-textarea-count` — `--font-caption-size`, `--color-text-secondary`
- `.acko-textarea-required` — `--color-error`

### Transitions
`border-color 200ms ease, box-shadow 200ms ease`

## React Props
- `resize` prop defaults to `"none"` (only valid value)
- `rows` defaults to 4
- The max-height CSS enforces scrolling regardless of `rows`
